# Git Workflow Commands

Git 工作流程命令，與 TaskMaster WBS 深度整合。

## 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    TaskMaster WBS                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │ Task-01 │  │ Task-02 │  │ Task-03 │  │ Task-04 │  ...  │
│  └────┬────┘  └─────────┘  └─────────┘  └─────────┘       │
└───────┼─────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────┐
│   /task-next      │  ← 從 WBS 選擇任務
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│   /start          │  ← 根據任務建立分支 feat/task-01-xxx
└────────┬──────────┘
         │
         ▼
    ┌────────────┐
    │  開發中... │
    └─────┬──────┘
          │ (多次)
          ▼
┌───────────────────┐
│   /commit         │  ← 本地檢查點
└────────┬──────────┘
         │
         ▼
┌───────────────────────────────────────┐
│   /pr (團隊)  或  /ship (Solo)        │
└────────┬──────────────────────────────┘
         │
         ▼
┌───────────────────┐
│  更新 WBS 狀態    │  ← 任務標記完成
└───────────────────┘
```

## 命令協作說明

| 命令 | 觸發時機 | WBS 整合 |
|------|----------|----------|
| `/start` | `/task-next` 後 | 讀取任務 ID 建立分支 |
| `/commit` | 開發過程中 | 可關聯任務進度 |
| `/pr` | 任務完成（團隊） | 更新任務為 Review |
| `/ship` | 任務完成（Solo） | 更新任務為 Done |

## 分支命名規則

根據 WBS 任務自動生成：

```
<type>/<task-id>-<description>

範例：
feat/task-01-user-login
fix/task-15-auth-bug
hotfix/task-20-security-patch
```

## 使用範例

### 完整工作流程

```bash
# 1. 查看下一個任務建議
/task-next

# 2. 開始任務（自動讀取 WBS）
/start                    # 自動使用 /task-next 推薦的任務
/start task=Task-05       # 或指定特定任務

# 3. 開發過程中提交
/commit                   # 多次

# 4. 完成任務
/pr                       # 團隊模式：建立 PR
/ship                     # Solo 模式：直接發布
```

## 安全等級

| 命令 | 風險 | 說明 |
|------|------|------|
| `/start` | 🟢 低 | 僅建立本地分支 |
| `/commit` | 🟢 低 | 僅本地提交 |
| `/pr` | 🟡 中 | 推送到遠端，需審查 |
| `/ship` | 🔴 高 | 直接修改 main，需確認 |
